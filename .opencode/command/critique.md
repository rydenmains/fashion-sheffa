---
description: Impeccable UX critique with heuristic scoring (default target: index.html)
agent: build
---

Run the impeccable `critique` workflow on the target ($ARGUMENTS, default index.html).

1. Load the `impeccable` skill via the skill tool.
2. Follow `reference/critique.md` exactly: dual isolated assessments, heuristic table, personas, priority issues, snapshot persistence.
3. Present the full report in chat.