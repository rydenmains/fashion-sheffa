---
description: Impeccable design workflow — critique, audit, polish, animate, bolder, etc.
agent: build
---

Run the impeccable `$1` workflow. Valid subcommands: critique, audit, polish, animate, bolder, quieter, distill, layout, typeset, colorize, delight, overdrive, clarify, adapt, optimize, shape, document, harden, onboard.

1. Load the `impeccable` skill via the skill tool.
2. Follow the matching `reference/<subcommand>.md` workflow exactly.
3. Target: $2 (default index.html). Present the full report in chat.