---
description: 'Use when you need to resolve an in-progress git merge/rebase conflict.'
agent: build
---

Invoke the `resolving-merge-conflicts` skill via the skill tool and follow its workflow. $ARGUMENTS
