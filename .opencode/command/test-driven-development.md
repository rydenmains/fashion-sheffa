---
description: Use when implementing any feature or bugfix, before writing implementation code
agent: build
---

Invoke the `test-driven-development` skill via the skill tool and follow its workflow. $ARGUMENTS
