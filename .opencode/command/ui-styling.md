---
description: Beautiful accessible UI with shadcn/Tailwind patterns
agent: build
---

Invoke the `ui-styling` skill via the skill tool and follow its workflow. Target: $ARGUMENTS (default: current page).